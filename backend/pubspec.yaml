name: inventy_backend
description: Inventy backend — Dart Frog API with hexagonal architecture.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.4.0
  dart_jsonwebtoken: ^2.14.0
  bcrypt: ^1.1.3

dev_dependencies:
  lints: ^4.0.0
  mocktail: ^1.0.4
  test: ^1.25.0
